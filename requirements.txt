# Shared deps for demo1 (notebook) and demo2 (notebook + pgvector).
# demo3 has its own requirements.txt (installed inside its docker image).
#   python -m venv venv && venv/bin/pip install -r requirements.txt
sentence-transformers
jupyter
psycopg[binary]
